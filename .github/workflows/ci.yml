name: .NET CI + Coverage (local report)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-coverage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup .NET 7
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build solution
        run: dotnet build --no-restore

      - name: Run tests with coverage and capture logs
        id: test
        continue-on-error: true
        run: |
          dotnet test tests/dotnet-ci.Tests.csproj --no-build --collect:"XPlat Code Coverage" 2>&1 | tee test-output.log
          echo "EXIT_CODE=${PIPESTATUS[0]}" >> $GITHUB_ENV

      - name: Install ReportGenerator
        run: dotnet tool install --global dotnet-reportgenerator-globaltool --version 5.1.26
        env:
          DOTNET_ROOT: /home/runner/.dotnet
          PATH: /home/runner/.dotnet/tools:/usr/bin:/bin

      - name: Generate coverage report
        run: |
          reportgenerator \
            -reports:tests/**/coverage.cobertura.xml \
            -targetdir:coverage-report \
            -reporttypes:Html
        env:
          PATH: /home/runner/.dotnet/tools:/usr/bin:/bin

      - name: Docker build
        run: docker build -t dotnet-ci-app .

      - name: Login to Docker Hub
        if: success()
        run: echo "${{ secrets.DOCKER_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Docker push
        if: success()
        run: |
          docker tag dotnet-ci-app ${{ secrets.DOCKER_USERNAME }}/dotnet-ci-app
          docker push ${{ secrets.DOCKER_USERNAME }}/dotnet-ci-app

      - name: Marcar como fallido si los tests fallaron
        if: env.EXIT_CODE != '0'
        run: exit 1

      - name: Enviar embed a Discord si pasa
        if: success()
        run: |
          curl -X POST -H "Content-Type: application/json" \
            -d '{
              "embeds": [{
                "title": "✅ CI exitoso",
                "description": "**Repositorio:** [${{ github.repository }}](https://github.com/${{ github.repository }})\n**Rama:** `${{ github.ref_name }}`\n[🔗 Ver run completo](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})",
                "color": 65280,
                "fields": [
                  { "name": "Commit", "value": "'"${{ github.event.head_commit.message }}"'" }
                ],
                "footer": {
                  "text": "Autor: ${{ github.actor }}"
                },
                "timestamp": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"
              }]
            }' \
            ${{ secrets.DISCORD_WEBHOOK_SUCCESS }}

      - name: IA explanation + Discord alert
        if: failure()
        run: |
          echo "Capturando logs..."
          if [ -f test-output.log ]; then
            LOGS=$(tail -n 50 test-output.log | head -c 1500 | sed 's/["`]/\\&/g' | tr '\n' ' ')
          else
            LOGS="No se pudo encontrar test-output.log"
          fi

          PROMPT=$(jq -Rs . <<< "Este es el log de un test fallido de .NET. Por favor, explicalo brevemente como si fueras una IA mentora para estudiantes, en un solo párrafo, usando lenguaje claro y corto. No repitas el stacktrace. Solo explica por qué falló, cuál fue el error esperado y cómo se soluciona.\n\n$LOGS")

          echo "Enviando a DeepSeek..."

          RESPONSE=$(curl -s https://openrouter.ai/api/v1/chat/completions \
            -H "Authorization: Bearer ${{ secrets.OPENROUTER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -H "HTTP-Referer: https://github.com/${{ github.repository }}" \
            -d "{
              \"model\": \"deepseek/deepseek-chat\",
              \"messages\": [
                {
                  \"role\": \"system\",
                  \"content\": \"Sos una IA que explica errores de testing de .NET para estudiantes nuevos.\"
                },
                {
                  \"role\": \"user\",
                  \"content\": $PROMPT
                }
              ]
            }")

          echo "Respuesta cruda:"
          echo "$RESPONSE"

          EXPLICACION=$(echo "$RESPONSE" | jq -r '.choices[0].message.content // "⚠️ DeepSeek no devolvió contenido válido."')
          SAFE_EXPLICACION=$(echo "$EXPLICACION" | head -c 1000 | jq -Rs .)
          COMMIT_MSG=$(echo "${{ github.event.head_commit.message }}" | head -c 200 | jq -Rs .)

          curl -X POST -H "Content-Type: application/json" \
            -d "{
              \"embeds\": [{
                \"title\": \"❌ Tests fallidos - explicación de IA\",
                \"description\": \"**Repositorio:** [${{ github.repository }}](https://github.com/${{ github.repository }})\\n**Rama:** \`${{ github.ref_name }}\`\\n[🔍 Ver run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})\",
                \"color\": 16711680,
                \"fields\": [
                  { \"name\": \"Commit\", \"value\": $COMMIT_MSG },
                  { \"name\": \"Explicación de la IA\", \"value\": $SAFE_EXPLICACION }
                ],
                \"footer\": { \"text\": \"Autor: ${{ github.actor }}\" },
                \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"
              }]
            }" \
            ${{ secrets.DISCORD_WEBHOOK_FAILURE }}
